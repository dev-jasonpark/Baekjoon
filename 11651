import sys
n=int(sys.stdin.readline())
cor=[]
for i in range(n):
    jum=list(map(int,sys.stdin.readline().split()))
    cor.append(jum)
cor.sort(key=lambda x:(x[1],x[0]))

for x,y in cor:
    print(x,y)
